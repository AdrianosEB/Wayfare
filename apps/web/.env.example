# Toggle the in-browser MSW fixture mock. Default: on in dev, off in prod build.
#   1 / true  → replay docs/fixtures via MSW (no server needed)
#   0 / false → talk to the live API through the Vite proxy (/api → :3000)
VITE_USE_MOCKS=1
